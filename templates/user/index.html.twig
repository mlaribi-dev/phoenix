{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
              
                <th>Email</th>
                <th>Prénom</th>
                <th>Nom</th>
                <th>Team</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                
                <td>{{ user.email }}</td>
                <td>{{ user.firstName }}</td>
                <td>{{ user.lastName }}</td>
                <td>{{ user.team.name }}</td>
                <td>
                    <a href="{{ path('user_show', {'id': user.id}) }}"><button type="button" class="btn btn-info">show</button></a>
                    <a href="{{ path('user_edit', {'id': user.id}) }}"><button type="button" class="btn btn-primary">edit</button></a>
                </td>
            </tr>
            
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('user_new') }}"><button type="button" class="btn btn-success">new</button></a>
    
{% endblock %}
