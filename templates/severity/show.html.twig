{% extends 'base.html.twig' %}

{% block title %}Severity{% endblock %}

{% block body %}
    <h1>Severity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ severity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ severity.name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ severity.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('severity_index') }}">back to list</a>

    <a href="{{ path('severity_edit', {'id': severity.id}) }}">edit</a>

    {{ include('severity/_delete_form.html.twig') }}
{% endblock %}
