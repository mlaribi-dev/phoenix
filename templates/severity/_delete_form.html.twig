<form method="post" action="{{ path('severity_delete', {'id': severity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ severity.id) }}">
    <button class="btn">Delete</button>
</form>
