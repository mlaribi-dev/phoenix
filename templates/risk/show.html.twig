{% extends 'base.html.twig' %}

{% block title %}Risk{% endblock %}

{% block body %}
    <h1>Risk</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ risk.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ risk.name }}</td>
            </tr>
            <tr>
                <th>Identification_date</th>
                <td>{{ risk.identificationDate ? risk.identificationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Resolved_date</th>
                <td>{{ risk.resolvedDate ? risk.resolvedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('risk_index') }}">back to list</a>

    <a href="{{ path('risk_edit', {'id': risk.id}) }}">edit</a>

    {{ include('risk/_delete_form.html.twig') }}
{% endblock %}
