<form method="post" action="{{ path('risk_delete', {'id': risk.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ risk.id) }}">
    <button class="btn">Delete</button>
</form>
