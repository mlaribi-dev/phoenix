{% extends 'base.html.twig' %}

{% block title %}Wallet{% endblock %}

{% block body %}
    <h1>Wallet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wallet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wallet.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wallet_index') }}">back to list</a>

    <a href="{{ path('wallet_edit', {'id': wallet.id}) }}">edit</a>

    {{ include('wallet/_delete_form.html.twig') }}
{% endblock %}
