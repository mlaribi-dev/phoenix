<form method="post" action="{{ path('fact_delete', {'id': fact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fact.id) }}">
    <button class="btn">Delete</button>
</form>
